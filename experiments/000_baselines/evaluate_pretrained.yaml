hydra:
  searchpath:
    - file://${oc.env:PWD}/configs
  sweep:
    dir: logs/experiments/000_baselines/evaluate_pretrained
    subdir: ${model_name}/${dataset_name}/seed=${seed}

model_name: ???
dataset_name: ???
batch_size: ???
seed: ???
limit_num_batches: null
log_to_file: false
