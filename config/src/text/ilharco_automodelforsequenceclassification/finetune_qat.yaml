model_name: ???
dataset_name: ???
seed: ???
batch_size: 128
lr: 1e-5
wd: 0.1
ls: 0.0
wl: 500           # warmup length (steps)
max_grad_norm: 1.0
max_length: 512
limit_num_batches: null
limit_num_epochs: null

qat:
  bits: ???
  granularity: ???
  skip_modules: ???

hydra:
  searchpath:
    - file://config
  run:
    dir: logs/src/text/ilharco_automodelforsequenceclassification/finetune_qat/${sanitize_hf:${model_name}}/${dataset_name}/seed=${seed}
  sweep:
    dir: logs/src/text/ilharco_automodelforsequenceclassification/finetune_qat
    subdir: ${sanitize_hf:${model_name}}/${dataset_name}/seed=${seed}
