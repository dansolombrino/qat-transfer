model_name: ???
dataset_name: ???
seed: ???
batch_size: 128
lr: 1e-5
wd: 0.1
ls: 0.0
wl: 500           # warmup length (steps)
max_grad_norm: 1.0
head_cache_dir: ${oc.env:HEAD_CACHE_DIR,heads}
limit_num_batches: null
limit_num_epochs: null

hydra:
  run:
    dir: logs/src/vision/ilharco_hf_clip/finetune_fp/${sanitize_hf:${model_name}}/${dataset_name}/seed=${seed}
  sweep:
    dir: logs/src/vision/ilharco_hf_clip/finetune_fp
    subdir: ${sanitize_hf:${model_name}}/${dataset_name}/seed=${seed}
